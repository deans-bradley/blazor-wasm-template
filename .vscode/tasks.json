{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build .NET Core API",
            "type": "shell",
            "command": "dotnet build ./API.csproj",
            "options": {
                "cwd": "${workspaceFolder}/API"
            },
            "problemMatcher": "$msCompile"
        }
    ]
}